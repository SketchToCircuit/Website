name: CI to Docker
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build stack
      - uses: sudo-bot/action-docker-compose@latest
      - with: 
          cli-arg: up -d
